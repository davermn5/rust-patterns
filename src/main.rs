fn main() {

    /* Updating a Box (heap) via a mutable reference (stack)
    let mut b: Box<i32> = Box::new(1);
    let c: &mut Box<i32> = &mut b;

    *c = Box::new(2); // OR **c = 2;

    println!("{c}");
     */
}
